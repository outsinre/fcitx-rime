# default.custom.yaml

patch:
  menu/page_size: 5
  switcher/hotkeys:
    - "Control+grave"
  ascii_composer:
    good_old_caps_lock: true
    switch_key:
      Caps_Lock: noop
      Eisu_toggle: clear
      Control_L: commit_text
      Control_R: commit_code
      Shift_L: inline_ascii
      Shift_R: noop # Fcitx 切换输入法
  schema_list:
    - schema: wubi06
    - schema: easy_en
    - schema: wuwei_pinyin
